# PROMPT: glossary_entry
# TYPE: few-shot
# USE: turn a new term encountered in a doc into a canonical glossary.md row
# INPUT: {term} — the term; {context} — sentence(s) where it appeared
# CONFIG: use model.temperature from config/config.yaml; max examples per limits.max_prompt_examples

Write a glossary table row for the term below, following the exact format of
the examples. The definition must be one sentence, project-specific, and the
"Not called" column must list likely wrong names to forbid.

Example 1:
Term: prod
Context: "deploy the container to prod after the checks pass"
Output: | prod | The production docker host at 192.168.1.10 running live services | live, main env, the server |

Example 2:
Term: CCU
Context: "the CCU spikes when backups run"
Output: | CCU | Concurrent connected users as reported by the nginx status module | active users, sessions |

Now the real input:
Term: {term}
Context: {context}
Output:
